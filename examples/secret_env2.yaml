apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: secret-as-env-var-2-
  annotations: {pipelines.kubeflow.org/kfp_sdk_version: 1.8.18, pipelines.kubeflow.org/pipeline_compilation_time: '2023-02-01T08:37:28.619283',
    pipelines.kubeflow.org/pipeline_spec: '{"name": "secret-as-env-var-2"}'}
  labels: {pipelines.kubeflow.org/kfp_sdk_version: 1.8.18}
spec:
  entrypoint: secret-as-env-var-2
  templates:
  - name: echo-secret
    container:
      command: [sh, -c, cat $SECRET_VAR]
      env:
      - {name: SECRET_VAR, value: /mnt/my_vol/password}
      image: alpine
      volumeMounts:
      - {mountPath: /mnt/my_vol, name: my-secret}
    metadata:
      labels:
        pipelines.kubeflow.org/kfp_sdk_version: 1.8.18
        pipelines.kubeflow.org/pipeline-sdk-type: kfp
        pipelines.kubeflow.org/enable_caching: "true"
    volumes:
    - name: my-secret
      secret: {secretName: my-secret}
  - name: secret-as-env-var-2
    dag:
      tasks:
      - {name: echo-secret, template: echo-secret}
  arguments:
    parameters: []
  serviceAccountName: pipeline-runner
