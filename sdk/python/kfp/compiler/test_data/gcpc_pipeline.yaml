components:
  comp-importer:
    executorLabel: exec-importer
    inputDefinitions:
      parameters:
        uri:
          parameterType: STRING
    outputDefinitions:
      artifacts:
        artifact:
          artifactType:
            schemaTitle: system.Artifact
            schemaVersion: 0.0.1
  comp-named-tuple-op:
    executorLabel: exec-named-tuple-op
    inputDefinitions:
      artifacts:
        artifact:
          artifactType:
            schemaTitle: system.Artifact
            schemaVersion: 0.0.1
    outputDefinitions:
      artifacts:
        model1:
          artifactType:
            schemaTitle: google.UnmanagedContainerModel
            schemaVersion: 0.0.1
        model2:
          artifactType:
            schemaTitle: google.UnmanagedContainerModel
            schemaVersion: 0.0.1
defaultPipelineRoot: gs://cjmccarthy-kfp-default-bucket
deploymentSpec:
  executors:
    exec-importer:
      importer:
        artifactUri:
          constant: gs://managed-pipeline-gcpc-e2e-test/automl-tabular/model
        metadata:
          containerSpec:
            imageUri: us-docker.pkg.dev/vertex-ai/automl-tabular/prediction-server:prod
        typeSchema:
          schemaTitle: google.UnmanagedContainerModel
          schemaVersion: 0.0.1
    exec-named-tuple-op:
      container:
        args:
        - --executor_input
        - '{{$}}'
        - --function_to_execute
        - named_tuple_op
        command:
        - sh
        - -c
        - "\nif ! [ -x \"$(command -v pip)\" ]; then\n    python3 -m ensurepip ||\
          \ python3 -m ensurepip --user || apt-get install python3-pip\nfi\n\nPIP_DISABLE_PIP_VERSION_CHECK=1\
          \ python3 -m pip install --quiet     --no-warn-script-location 'git+https://github.com/connor-mccarthy/pipelines@support-custom-artifact-types#subdirectory=components/google-cloud'\
          \ 'git+https://github.com/connor-mccarthy/pipelines@support-custom-artifact-types#subdirectory=sdk/python'\
          \ && \"$0\" \"$@\"\n"
        - sh
        - -ec
        - 'program_path=$(mktemp -d)

          printf "%s" "$0" > "$program_path/ephemeral_component.py"

          python3 -m kfp.components.executor_main                         --component_module_path                         "$program_path/ephemeral_component.py"                         "$@"

          '
        - "\nimport kfp\nfrom kfp import dsl\nfrom kfp.dsl import *\nfrom typing import\
          \ *\n\ndef named_tuple_op(\n    artifact: Input[dsl.Artifact]\n) -> NamedTuple('Outputs',\
          \ [('model1', artifact_types.UnmanagedContainerModel),\n               \
          \             ('model2', artifact_types.UnmanagedContainerModel)\n     \
          \                      ]):\n    import collections\n\n    output = collections.namedtuple('Outputs',\
          \ ['model1', 'model2'])\n    return output(model1=artifact, model2=artifact)\n\
          \n"
        image: python:3.7
pipelineInfo:
  name: test-custom-artifact
root:
  dag:
    tasks:
      importer:
        cachingOptions:
          enableCache: true
        componentRef:
          name: comp-importer
        inputs:
          parameters:
            uri:
              runtimeValue:
                constant: gs://managed-pipeline-gcpc-e2e-test/automl-tabular/model
        taskInfo:
          name: importer
      named-tuple-op:
        cachingOptions:
          enableCache: true
        componentRef:
          name: comp-named-tuple-op
        dependentTasks:
        - importer
        inputs:
          artifacts:
            artifact:
              taskOutputArtifact:
                outputArtifactKey: artifact
                producerTask: importer
        taskInfo:
          name: named-tuple-op
schemaVersion: 2.1.0
sdkVersion: kfp-2.0.0-alpha.5
